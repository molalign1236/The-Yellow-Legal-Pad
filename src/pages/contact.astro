---
import BaseLayout from "../layouts/BaseLayout.astro";

// Contact page.
//
// Per relay #37 §8: contact flow should feel emotionally easy.
// Plain language. No aggressive required fields. Reassurance
// copy. "No pressure" language.
//
// Pass 315 removed an unreliable mailto: form. The email link is
// now the only path — it always works. A real form (Calendly,
// Formspree, or a Vercel serverless function) can be added when
// there's a backend; for v1, an email link is honest UX.
---

<BaseLayout
  title="Contact"
  description="Book a Discovery Call with Stacey. Short, free, unhurried, and no pressure to continue."
>
  <!-- =====================================================
       Reassurance opening
       ===================================================== -->
  <section class="section reveal-on-scroll" aria-labelledby="contact-heading">
    <div class="column">
      <h1 id="contact-heading">Book a Discovery Call</h1>

      <p>
        Send Stacey a note. She reads every message herself, usually
        within a day or two, and she responds &mdash; even if it's just
        to say she's not the right fit. No autoresponders. No drip
        emails. Just a person.
      </p>

      <p>
        A Discovery Call is short (about twenty minutes), free, and
        unhurried. There's no obligation to continue afterward.
      </p>
    </div>
  </section>

  <!-- =====================================================
       Email link
       The primary path. A form arrives later, once there's a
       reliable backend (Calendly, Formspree, or a Vercel
       serverless function). For now: just an email link, which
       always works.
       ===================================================== -->
  <section class="section reveal-on-scroll" aria-labelledby="email-heading">
    <div class="column">
      <h2 id="email-heading">Email Stacey</h2>

      <p>
        Send a short note. Or a long one. Anything you write is fine.
      </p>

      <p class="contact-email">
        <a href="mailto:hello@yellowlegalpad.example?subject=Discovery%20Call">
          hello@yellowlegalpad.example
        </a>
      </p>
    </div>
  </section>

  <!-- =====================================================
       Reassurance closing
       ===================================================== -->
  <section class="section reveal-on-scroll" aria-labelledby="closing-heading">
    <div class="column">
      <h2 id="closing-heading">Before you write</h2>

      <p>
        You don't need to summarize your whole financial picture.
        You don't need to know what you want. "I've been meaning
        to talk to someone about money" is enough.
      </p>

      <p>
        And if you write and never end up booking a session, that
        is also fine. Stacey isn't tracking conversion rates.
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  /* The email link sits a touch larger than body text — enough
     presence to be obviously the call to action without becoming
     a button. Underline is the existing gold convention. */
  .contact-email {
    margin-top: 1.5rem;
    font-size: 1.25rem;
  }
</style>
